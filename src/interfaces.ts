interface CreepMemory {
    harvesting?: boolean;
    role?: string;
};
interface FlagMemory { };
interface SpawnMemory { };
interface RoomMemory { };
interface MyMemory {
    globalId: number;
    rclStage: number;
};
